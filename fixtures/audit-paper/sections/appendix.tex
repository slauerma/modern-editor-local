\section{Endpoint check}\label{sec:endpoint}
Section~\ref{sec:rule} specifies the allocation, and Proposition~\ref{prop:optimal} establishes pointwise optimality.
At the boundary, $x(0)=0$, while $x(\theta)>0$ for every $\theta\in(0,1]$.
This included source file checks that relative inputs and references survive the build snapshot.
\begin{equation}
  x(\theta')-x(\theta)=\theta'-\theta>0
  \qquad\text{when }\theta'>\theta.
\end{equation}
The local bibliography below contains one synthetic record, not a cited research source.
